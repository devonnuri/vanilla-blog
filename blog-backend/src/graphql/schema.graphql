scalar Date

type Post {
    title: String!
    body: String!
    createdAt: Date!
}

type Query {
    post(title: String, body: String): [Post]!
}

type Mutation {
    addPost(title: String!, body: String!): Post
}