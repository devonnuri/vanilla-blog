scalar Date

type Post {
    id: Int!
    title: String!
    body: String!
    createdAt: Date!
}

type Query {
    post(id: Int, title: String, body: String): [Post]!
}

type Mutation {
    addPost(title: String!, body: String!): Post
}