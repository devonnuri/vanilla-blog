import React from 'react';

import styled from 'styled-components';

const Header = styled.div`
  text-align: center;
`;

export default () => {
  return (
    <Header>
      <h1>뎁온누리</h1>
    </Header>
  );
};
